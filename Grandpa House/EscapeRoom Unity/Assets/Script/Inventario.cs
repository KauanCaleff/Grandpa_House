using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Inventario : MonoBehaviour
{
    /* public List<Item> itens;

     public void AddItem(Item item)
     {
         if (itens.Contains(item))
         {
             return;
         }
         UImanager.instance.SetItens(item, itens.Count);
         itens.Add(item);

     if (currentInteractable.itens.invetarioItem) { Inventario.AddItem(currentInteractable.itens); // RuntimeManager.PlayOneShot("event:/UI/ItemColetado"); currentInteractable.CollectItem.Invoke(); }

     }*/
}
